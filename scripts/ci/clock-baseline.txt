# Files that still read the wall clock directly.
#
# The rule (io.crossasset.ems.core.clock.Clock, since task 3.6) is that business
# logic takes a TimeSource. scripts/ci/checks/no_raw_clock.py enforces it, and
# this file is the exception list. It can shrink; it must not grow.
#
# Every entry below is either an I/O deadline — genuinely wall-clock work, with
# nothing to do with business time — or a demo/CLI entry point that is not
# replayed and not part of the conformance gate. If you add a file here, say in
# the PR which of those two it is.
#
# The four services this list used to contain — InMemoryAaaService,
# InMemoryStagedOrderManager, InMemoryRouteManager and
# InMemoryMultiLegOrderManager — now take a TimeSource. Their javadoc had
# claimed injection for months while the code read System.currentTimeMillis().

# I/O deadlines around Aeron offers, archive replay and cluster bootstrap.
# Wall-clock timeouts on network operations, not business timestamps.
java/ems-transport/src/main/java/io/crossasset/ems/transport/AeronClusterBootstrap.java
java/ems-transport/src/main/java/io/crossasset/ems/transport/AeronArchiveReplayService.java

# Demo entry points. Not replayed, not in the slice, not in the gate.
java/ems-transport/src/main/java/io/crossasset/ems/transport/AeronToyPingPong.java
java/ems-fix-bridge/src/main/java/io/crossasset/ems/api/rest/TraderDesktopEdgeMain.java
java/ems-fix-bridge/src/main/java/io/crossasset/ems/api/rest/RestEdgeBinding.java

# Vendor driver shim. This one is a genuine business timestamp (the tick time
# handed to onTick), not an I/O deadline — so it is a real debt, not an
# exemption on merit. ems-market-data is outside the cash-equity slice
# (ADR 0002), so it is baselined rather than fixed here; fix it when the module
# is next touched.
java/ems-market-data/src/main/java/io/crossasset/ems/md/bloomberg/ReflectiveBlpapiDriver.java
